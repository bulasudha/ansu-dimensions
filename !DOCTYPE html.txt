<!DOCTYPE html>
<html lang="si">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ansu Dimensions | Premium</title>
    <!-- Importing a beautiful modern font from Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;800&display=swap" rel="stylesheet">
    <style>
        /* Color Palette & Variables */
        :root {
            --primary-glow: #00f0ff;
            --secondary-glow: #8a2be2;
            --bg-color: #050510;
            --glass-bg: rgba(255, 255, 255, 0.03);
            --glass-border: rgba(255, 255, 255, 0.1);
        }

        body {
            font-family: 'Poppins', sans-serif;
            margin: 0;
            background-color: var(--bg-color);
            color: #ffffff;
            /* Cosmic background gradients */
            background-image: 
                radial-gradient(circle at 15% 50%, rgba(138, 43, 226, 0.2), transparent 25%),
                radial-gradient(circle at 85% 30%, rgba(0, 240, 255, 0.2), transparent 25%);
            background-attachment: fixed;
            overflow-x: hidden;
        }

        /* Animated Navigation Bar */
        nav {
            display: flex;
            justify-content: center;
            gap: 40px;
            padding: 20px;
            background: rgba(5, 5, 16, 0.8);
            backdrop-filter: blur(10px);
            position: sticky;
            top: 0;
            z-index: 1000;
            border-bottom: 1px solid var(--glass-border);
        }

        nav a {
            color: #fff;
            text-decoration: none;
            font-weight: 600;
            letter-spacing: 1px;
            text-transform: uppercase;
            font-size: 0.9em;
            transition: color 0.3s, text-shadow 0.3s;
        }

        nav a:hover {
            color: var(--primary-glow);
            text-shadow: 0 0 10px var(--primary-glow);
        }

        /* Hero Section */
        .hero {
            text-align: center;
            padding: 120px 20px 80px;
        }

        .hero h1 {
            font-size: 4.5em;
            margin: 0;
            font-weight: 800;
            letter-spacing: 2px;
            /* Gradient Text Effect */
            background: linear-gradient(90deg, var(--primary-glow), var(--secondary-glow));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            animation: pulse 3s infinite alternate;
        }

        .hero p {
            font-size: 1.2em;
            color: #b0b0c0;
            max-width: 600px;
            margin: 20px auto;
            line-height: 1.6;
        }

        /* Glassmorphism Video Grid */
        .container {
            max-width: 1100px;
            margin: 0 auto;
            padding: 40px 20px;
        }

        .section-title {
            text-align: center;
            font-size: 2.5em;
            margin-bottom: 50px;
            color: #fff;
            text-shadow: 0 0 15px rgba(255, 255, 255, 0.3);
        }

        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
        }

        .card {
            background: var(--glass-bg);
            backdrop-filter: blur(16px);
            border: 1px solid var(--glass-border);
            border-radius: 20px;
            padding: 20px;
            transition: transform 0.4s ease, box-shadow 0.4s ease, border-color 0.4s;
        }

        .card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 35px rgba(138, 43, 226, 0.3);
            border-color: var(--secondary-glow);
        }

        .card-img {
            width: 100%;
            height: 180px;
            background: #111;
            border-radius: 12px;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 0.9em;
            color: #666;
            background-image: linear-gradient(45deg, #1a1a24, #0d0d14);
        }

        .card h3 {
            margin: 0 0 10px;
            font-size: 1.2em;
            line-height: 1.4;
            color: #e0e0ff;
        }

        /* Social Buttons */
        .social-container {
            text-align: center;
            padding: 60px 20px;
            background: linear-gradient(to top, #000, transparent);
        }

        .social-btn {
            display: inline-block;
            padding: 15px 40px;
            margin: 10px;
            border-radius: 30px;
            font-weight: 600;
            text-decoration: none;
            color: white;
            text-transform: uppercase;
            letter-spacing: 1px;
            transition: all 0.3s ease;
            border: 2px solid transparent;
        }

        .youtube {
            background: rgba(255, 0, 0, 0.1);
            border-color: #ff0000;
            box-shadow: 0 0 15px rgba(255, 0, 0, 0.3);
        }
        .youtube:hover { background: #ff0000; box-shadow: 0 0 25px #ff0000; transform: scale(1.05); }

        .tiktok {
            background: rgba(37, 244, 238, 0.1);
            border-color: #25F4EE;
            box-shadow: 0 0 15px rgba(37, 244, 238, 0.3);
        }
        .tiktok:hover { background: #25F4EE; color: #000; box-shadow: 0 0 25px #25F4EE; transform: scale(1.05); }

        .facebook {
            background: rgba(24, 119, 242, 0.1);
            border-color: #1877F2;
            box-shadow: 0 0 15px rgba(24, 119, 242, 0.3);
        }
        .facebook:hover { background: #1877F2; box-shadow: 0 0 25px #1877F2; transform: scale(1.05); }

        /* Keyframe Animations */
        @keyframes pulse {
            0% { text-shadow: 0 0 20px rgba(0, 240, 255, 0.4); }
            100% { text-shadow: 0 0 40px rgba(138, 43, 226, 0.8); }
        }
    </style>
</head>
<body>

    <nav>
        <a href="#home">Home</a>
        <a href="#videos">Dimensions</a>
        <a href="#connect">Connect</a>
    </nav>

    <div class="hero" id="home">
        <h1>Ansu Dimensions</h1>
        <p>මේ ලෝකයේ සහ විශ්වයේ තිබෙන සුවිශේෂීම, විශ්මිත සහ අභිරහස් දේවල් පිළිබඳව ගවේෂණය කරන්න.</p>
    </div>

    <div class="container" id="videos">
        <h2 class="section-title">Latest Explorations</h2>
        <div class="grid">
            
            <div class="card">
                <div class="card-img">Jurassic Era Thumbnail Placeholder</div>
                <h3>ජුරාසික් යුගයේ ලෝකය | මිනිසුන්ට පෙර පෘථිවිය පාලනය කළ දැවැන්තයින්!</h3>
            </div>
            
            <div class="card">
                <div class="card-img">Motivation Thumbnail Placeholder</div>
                <h3>ඉංග්‍රීසි බැරිද? ඒක ලැජ්ජාවක් නෙවෙයි! | Motivation to Learn English</h3>
            </div>
            
            <div class="card">
                <div class="card-img">AI Guide Thumbnail Placeholder</div>
                <h3>Ultimate AI Guide 2026 | කියවන දේ අමතක වෙනවාද?</h3>
            </div>
            
        </div>
    </div>

    <div class="social-container" id="connect">
        <h2 style="margin-bottom: 40px; color: #fff;">Enter The Dimension</h2>
        <a href="https://www.youtube.com/@AnsuDimensions2" target="_blank" class="social-btn youtube">YouTube</a>
        <a href="#" class="social-btn tiktok">TikTok</a>
        <a href="#" class="social-btn facebook">Facebook</a>
    </div>

</body>
</html>
