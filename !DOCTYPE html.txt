<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ansu Dimensions | AI Video Creator & Merch Store</title>
    <!-- Import Google Fonts & FontAwesome Icons -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Rajdhani:wght@500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    
    <style>
        /* CSS RESET & CUSTOM VARIABLES */
        :root {
            --bg-color: #050508;
            --panel-color: #0d0d15;
            --neon-blue: #00f0ff;
            --neon-pink: #ff007f;
            --text-white: #f5f5fa;
            --text-gray: #8a8a9d;
            --glow-blue: 0 0 10px rgba(0, 240, 255, 0.5), 0 0 20px rgba(0, 240, 255, 0.2);
            --glow-pink: 0 0 10px rgba(255, 0, 127, 0.5), 0 0 20px rgba(255, 0, 127, 0.2);
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-white);
            font-family: 'Rajdhani', sans-serif;
            font-size: 18px;
            overflow-x: hidden;
            scroll-behavior: smooth;
        }

        /* SCROLLBAR CUSTOMIZATION */
        ::-webkit-scrollbar {
            width: 8px;
        }
        ::-webkit-scrollbar-track {
            background: var(--bg-color);
        }
        ::-webkit-scrollbar-thumb {
            background: var(--panel-color);
            border: 2px solid var(--neon-blue);
            border-radius: 4px;
        }

        /* HEADER & NAVIGATION */
        header {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            background: rgba(5, 5, 8, 0.85);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(0, 240, 255, 0.2);
            padding: 20px 40px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 1000;
        }

        .logo {
            font-family: 'Orbitron', sans-serif;
            font-weight: 900;
            font-size: 24px;
            letter-spacing: 2px;
            background: linear-gradient(45deg, var(--neon-blue), var(--neon-pink));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            text-shadow: var(--glow-blue);
            cursor: pointer;
        }

        nav a {
            color: var(--text-white);
            text-decoration: none;
            font-weight: 600;
            margin-left: 30px;
            font-size: 18px;
            letter-spacing: 1px;
            text-transform: uppercase;
            transition: all 0.3s ease;
            position: relative;
        }

        nav a::after {
            content: '';
            position: absolute;
            width: 0;
            height: 2px;
            bottom: -5px;
            left: 0;
            background-color: var(--neon-blue);
            transition: width 0.3s ease;
        }

        nav a:hover {
            color: var(--neon-blue);
            text-shadow: var(--glow-blue);
        }

        nav a:hover::after {
            width: 100%;
        }

        /* HERO SECTION */
        .hero {
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 0 20px;
            position: relative;
            background: radial-gradient(circle at center, rgba(0, 240, 255, 0.08) 0%, rgba(5, 5, 8, 1) 70%);
            border-bottom: 1px solid rgba(255, 0, 127, 0.1);
        }

        /* Abstract Glow Blobs in Background */
        .hero::before {
            content: '';
            position: absolute;
            width: 300px;
            height: 300px;
            background: var(--neon-blue);
            filter: blur(150px);
            opacity: 0.15;
            top: 20%;
            left: 15%;
            pointer-events: none;
        }

        .hero::after {
            content: '';
            position: absolute;
            width: 300px;
            height: 300px;
            background: var(--neon-pink);
            filter: blur(150px);
            opacity: 0.15;
            bottom: 20%;
            right: 15%;
            pointer-events: none;
        }

        .hero-tag {
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 4px;
            color: var(--neon-pink);
            font-weight: 700;
            margin-bottom: 15px;
            font-family: 'Orbitron', sans-serif;
            text-shadow: var(--glow-pink);
        }

        .hero h1 {
            font-family: 'Orbitron', sans-serif;
            font-size: 72px;
            font-weight: 900;
            letter-spacing: 2px;
            margin-bottom: 20px;
            background: linear-gradient(to right, #fff 40%, var(--neon-blue));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .hero p {
            font-size: 22px;
            max-width: 600px;
            color: var(--text-gray);
            margin-bottom: 40px;
            line-height: 1.6;
        }

        .cta-btn {
            background: transparent;
            color: var(--neon-blue);
            border: 2px solid var(--neon-blue);
            padding: 15px 35px;
            font-size: 18px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 2px;
            border-radius: 5px;
            cursor: pointer;
            box-shadow: inset 0 0 0 0 rgba(0, 240, 255, 0.2);
            transition: all 0.4s ease;
            font-family: 'Orbitron', sans-serif;
        }

        .cta-btn:hover {
            background: var(--neon-blue);
            color: #000;
            box-shadow: 0 0 15px rgba(0, 240, 255, 0.6);
            text-shadow: none;
        }

        /* SECTION LAYOUTS */
        section {
            padding: 100px 40px 60px;
            max-width: 1200px;
            margin: 0 auto;
        }

        .section-header {
            text-align: center;
            margin-bottom: 60px;
        }

        .section-header h2 {
            font-family: 'Orbitron', sans-serif;
            font-size: 40px;
            text-transform: uppercase;
            letter-spacing: 2px;
            display: inline-block;
            position: relative;
            padding-bottom: 15px;
        }

        .section-header h2::after {
            content: '';
            position: absolute;
            width: 60px;
            height: 3px;
            background: var(--neon-pink);
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            box-shadow: var(--glow-pink);
        }

        .section-header p {
            color: var(--text-gray);
            margin-top: 15px;
            font-size: 18px;
        }

        /* AI VIDEOS GRID */
        .video-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 30px;
        }

        .video-card {
            background-color: var(--panel-color);
            border: 1px solid rgba(255, 255, 255, 0.05);
            border-radius: 12px;
            overflow: hidden;
            transition: all 0.3s ease;
            position: relative;
        }

        .video-card:hover {
            transform: translateY(-8px);
            border-color: var(--neon-blue);
            box-shadow: var(--glow-blue);
        }

        .video-thumbnail {
            position: relative;
            height: 200px;
            background-size: cover;
            background-position: center;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .video-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(13, 13, 21, 0.7);
            transition: background 0.3s ease;
        }

        .video-card:hover .video-overlay {
            background: rgba(13, 13, 21, 0.4);
        }

        .play-btn {
            font-size: 48px;
            color: var(--neon-blue);
            z-index: 2;
            transition: transform 0.3s ease;
            cursor: pointer;
        }

        .video-card:hover .play-btn {
            transform: scale(1.2);
            color: var(--neon-pink);
        }

        .platform-badge {
            position: absolute;
            top: 15px;
            right: 15px;
            background: rgba(0, 0, 0, 0.8);
            border: 1px solid var(--neon-pink);
            padding: 5px 12px;
            border-radius: 20px;
            font-size: 12px;
            text-transform: uppercase;
            font-weight: bold;
            color: var(--neon-pink);
            z-index: 2;
        }

        .video-info {
            padding: 25px;
        }

        .video-info h3 {
            font-size: 22px;
            font-family: 'Orbitron', sans-serif;
            margin-bottom: 10px;
        }

        .video-info p {
            color: var(--text-gray);
            font-size: 15px;
            line-height: 1.5;
        }

        /* MERCH SHOP GRID */
        .shop-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 40px;
        }

        .product-card {
            background-color: var(--panel-color);
            border: 1px solid rgba(255, 255, 255, 0.05);
            border-radius: 16px;
            overflow: hidden;
            transition: all 0.3s ease;
        }

        .product-card:hover {
            transform: translateY(-8px);
            border-color: var(--neon-pink);
            box-shadow: var(--glow-pink);
        }

        .product-img-wrapper {
            height: 320px;
            overflow: hidden;
            position: relative;
            background-color: #12121e;
        }

        .product-img-wrapper img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.5s ease;
        }

        .product-card:hover .product-img-wrapper img {
            transform: scale(1.1);
        }

        .product-info {
            padding: 25px;
            text-align: center;
        }

        .product-tag {
            font-size: 12px;
            text-transform: uppercase;
            letter-spacing: 2px;
            color: var(--neon-blue);
            margin-bottom: 8px;
            display: block;
        }

        .product-info h3 {
            font-family: 'Orbitron', sans-serif;
            font-size: 24px;
            margin-bottom: 10px;
        }

        .product-price {
            font-size: 20px;
            color: var(--neon-pink);
            font-weight: 700;
            margin-bottom: 20px;
        }

        .order-btn {
            width: 100%;
            background: linear-gradient(45deg, var(--neon-pink), #b0005d);
            color: #fff;
            border: none;
            padding: 12px 0;
            font-size: 16px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }

        .order-btn:hover {
            box-shadow: var(--glow-pink);
            transform: scale(1.02);
        }

        /* ABOUT / VS CODE SECTION */
        .about-container {
            display: flex;
            align-items: center;
            gap: 50px;
            background: rgba(13, 13, 21, 0.5);
            border: 1px solid rgba(0, 240, 255, 0.1);
            padding: 40px;
            border-radius: 16px;
        }

        .about-text {
            flex: 1;
        }

        .about-text h3 {
            font-family: 'Orbitron', sans-serif;
            font-size: 32px;
            color: var(--neon-blue);
            margin-bottom: 20px;
        }

        .about-text p {
            color: var(--text-gray);
            line-height: 1.7;
            margin-bottom: 20px;
        }

        .vscode-visual {
            flex: 1;
            background-color: #1e1e1e;
            border-radius: 10px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
            font-family: 'Courier New', Courier, monospace;
            font-size: 14px;
            overflow: hidden;
            border: 1px solid #333;
        }

        .vscode-header {
            background-color: #323233;
            padding: 10px;
            display: flex;
            gap: 8px;
            align-items: center;
        }

        .vscode-dot {
            width: 12px;
            height: 12px;
            border-radius: 50%;
        }
        .dot-red { background-color: #ff5f56; }
        .dot-yellow { background-color: #ffbd2e; }
        .dot-green { background-color: #27c93f; }

        .vscode-title {
            color: #9da5b4;
            margin-left: 20px;
            font-size: 12px;
        }

        .vscode-body {
            padding: 20px;
            color: #abb2bf;
            line-height: 1.5;
        }

        .code-keyword { color: #c678dd; }
        .code-string { color: #98c379; }
        .code-var { color: #e06c75; }
        .code-function { color: #61afef; }
        .code-comment { color: #5c6370; font-style: italic; }

        /* FOOTER */
        footer {
            background-color: #030305;
            border-top: 1px solid rgba(0, 240, 255, 0.1);
            padding: 50px 40px;
            text-align: center;
        }

        .footer-socials {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-bottom: 30px;
        }

        .social-icon {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            border: 1px solid rgba(0, 240, 255, 0.3);
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--neon-blue);
            font-size: 20px;
            transition: all 0.3s ease;
            text-decoration: none;
        }

        .social-icon:hover {
            background-color: var(--neon-blue);
            color: #000;
            box-shadow: var(--glow-blue);
            transform: translateY(-5px);
        }

        .footer-logo {
            font-family: 'Orbitron', sans-serif;
            font-weight: 700;
            font-size: 20px;
            color: #fff;
            margin-bottom: 15px;
        }

        .footer-copy {
            color: var(--text-gray);
            font-size: 14px;
        }

        /* RESPONSIVENESS */
        @media (max-width: 900px) {
            .about-container {
                flex-direction: column;
            }
            .hero h1 {
                font-size: 48px;
            }
            header {
                padding: 15px 20px;
            }
            nav a {
                margin-left: 15px;
                font-size: 15px;
            }
        }
    </style>
</head>
<body>

    <!-- STICKY HEADER -->
    <header>
        <div class="logo">ANSU DIMENSIONS</div>
        <nav>
            <a href="#home">Home</a>
            <a href="#videos">AI Videos</a>
            <a href="#shop">Store</a>
            <a href="#about">About</a>
        </nav>
    </header>

    <!-- HERO SECTION -->
    <div class="hero" id="home">
        <div class="hero-tag">Entering New Dimensions</div>
        <h1>ANSU DIMENSIONS</h1>
        <p>Creating futuristic AI animations and high-end tech-wear merchandise. Designed in VS Code, powered by pure imagination.</p>
        <button class="cta-btn" onclick="scrollToShop()">Explore Store</button>
    </div>

    <!-- AI VIDEOS SECTION -->
    <section id="videos">
        <div class="section-header">
            <h2>AI Creations</h2>
            <p>High-quality immersive AI videos curated for TikTok and YouTube.</p>
        </div>
        <div class="video-grid">
            <!-- Video Card 1 -->
            <div class="video-card">
                <div class="video-thumbnail" style="background-image: url('http://googleusercontent.com/image_collection/image_retrieval/3924824769309083368_0');">
                    <div class="video-overlay"></div>
                    <div class="platform-badge">TikTok</div>
                    <div class="play-btn" onclick="openSocialLink('tiktok')"><i class="fa-solid fa-play"></i></div>
                </div>
                <div class="video-info">
                    <h3>Futuristic Cyber Dream</h3>
                    <p>A mesmerizing AI-generated animation depicting cyberpunk cityscapes with neon lighting.</p>
                </div>
            </div>
            <!-- Video Card 2 -->
            <div class="video-card">
                <div class="video-thumbnail" style="background-image: url('http://googleusercontent.com/image_collection/image_retrieval/3924824769309083368_1');">
                    <div class="video-overlay"></div>
                    <div class="platform-badge">YouTube</div>
                    <div class="play-btn" onclick="openSocialLink('youtube')"><i class="fa-solid fa-play"></i></div>
                </div>
                <div class="video-info">
                    <h3>Abstract AI Dimensions</h3>
                    <p>Exploring complex fractal dimensions using advanced neural style transfer models.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- SHOP SECTION -->
    <section id="shop">
        <div class="section-header">
            <h2>Dimension Store</h2>
            <p>Secure your exclusive Ansu Dimensions merchandise. Custom printed and designed.</p>
        </div>
        <div class="shop-grid">
            <!-- Product 1: T-Shirt -->
            <div class="product-card">
                <div class="product-img-wrapper">
                    <!-- High quality streetwear tshirt mockup -->
                    <img src="http://googleusercontent.com/image_collection/image_retrieval/12213693526356049537_0" alt="Ansu Dimensions Cyber T-Shirt">
                </div>
                <div class="product-info">
                    <span class="product-tag">CLOTHING</span>
                    <h3>Official Ansu Cyber Tee</h3>
                    <p class="product-price">LKR 1,800.00</p>
                    <button class="order-btn" onclick="orderViaWhatsApp('Official Ansu Cyber Tee', 'LKR 1,800.00')">
                        <i class="fa-brands fa-whatsapp"></i> Order via WhatsApp
                    </button>
                </div>
            </div>
            <!-- Product 2: Ring & Necklace -->
            <div class="product-card">
                <div class="product-img-wrapper">
                    <!-- High quality ring/jewelry aesthetic -->
                    <img src="http://googleusercontent.com/image_collection/image_retrieval/3924824769309083368_3" alt="Cyber Ring & Pendant">
                </div>
                <div class="product-info">
                    <span class="product-tag">ACCESSORIES</span>
                    <h3>Dimension Ring & Necklace Set</h3>
                    <p class="product-price">LKR 950.00</p>
                    <button class="order-btn" onclick="orderViaWhatsApp('Dimension Ring & Necklace Set', 'LKR 950.00')">
                        <i class="fa-brands fa-whatsapp"></i> Order via WhatsApp
                    </button>
                </div>
            </div>
        </div>
    </section>

    <!-- ABOUT SECTION (VS Code Integration) -->
    <section id="about">
        <div class="section-header">
            <h2>Creative Lab</h2>
            <p>How we bring design, code, and AI technology together inside VS Code.</p>
        </div>
        <div class="about-container">
            <div class="about-text">
                <h3>Code Meets Creativity</h3>
                <p>Ansu Dimensions is more than a brand—it is a creative ecosystem. Using Visual Studio Code, we construct modern interactive experiences to bridge the gap between artificial intelligence art and physical streetwear products.</p>
                <p>By blending web programming (HTML/CSS) with video generation tools, we create a futuristic retail and content Hub.</p>
            </div>
            <div class="vscode-visual">
                <div class="vscode-header">
                    <div class="vscode-dot dot-red"></div>
                    <div class="vscode-dot dot-yellow"></div>
                    <div class="vscode-dot dot-green"></div>
                    <div class="vscode-title">editor.js - Visual Studio Code</div>
                </div>
                <div class="vscode-body">
                    <p><span class="code-comment">// Editing Ansu Dimensions Platform</span></p>
                    <p><span class="code-keyword">const</span> <span class="code-var">brand</span> = <span class="code-string">"Ansu Dimensions"</span>;</p>
                    <p><span class="code-keyword">const</span> <span class="code-var">tools</span> = [<span class="code-string">"AI Video"</span>, <span class="code-string">"T-shirts"</span>, <span class="code-string">"VS Code"</span>];</p>
                    <br>
                    <p><span class="code-keyword">function</span> <span class="code-function">renderVibe</span>() {</p>
                    <p>&nbsp;&nbsp;<span class="code-var">console</span>.<span class="code-function">log</span>(<span class="code-string">`Launching ${brand}...`</span>);</p>
                    <p>&nbsp;&nbsp;<span class="code-keyword">return</span> <span class="code-string">"🚀 Futuristic Glow Active!"</span>;</p>
                    <p>}</p>
                    <br>
                    <p><span class="code-function">renderVibe</span>();</p>
                </div>
            </div>
        </div>
    </section>

    <!-- FOOTER -->
    <footer>
        <div class="footer-socials">
            <a href="https://tiktok.com" target="_blank" class="social-icon"><i class="fa-brands fa-tiktok"></i></a>
            <a href="https://youtube.com" target="_blank" class="social-icon"><i class="fa-brands fa-youtube"></i></a>
            <a href="https://instagram.com" target="_blank" class="social-icon"><i class="fa-brands fa-instagram"></i></a>
        </div>
        <div class="footer-logo">ANSU DIMENSIONS</div>
        <p class="footer-copy">© 2026 Ansu Dimensions. Created using Visual Studio Code.</p>
    </footer>

    <!-- INTERACTION SCRIPTS -->
    <script>
        // Smooth scroll to store section
        function scrollToShop() {
            document.getElementById('shop').scrollIntoView({ behavior: 'smooth' });
        }

        // WhatsApp direct ordering link generator
        function orderViaWhatsApp(itemName, price) {
            // Replace this number with your actual WhatsApp phone number (with country code, e.g., 94771234567 for Sri Lanka)
            const myPhoneNumber = "94771234567"; 
            
            const message = `Hello Ansu Dimensions! 🚀 I would like to order:\n\n🛍️ Product: *${itemName}*\n💰 Price: *${price}*\n\nPlease let me know the delivery details. Thank you!`;
            const encodedMessage = encodeURIComponent(message);
            const whatsappURL = `https://wa.me/${myPhoneNumber}?text=${encodedMessage}`;
            
            window.open(whatsappURL, '_blank');
        }

        // Redirect mock social interactions
        function openSocialLink(platform) {
            if (platform === 'tiktok') {
                alert("This will direct to your TikTok Video Link! You can easily change this link in VS Code.");
            } else if (platform === 'youtube') {
                alert("This will direct to your YouTube Video Link! You can easily change this link in VS Code.");
            }
        }
    </script>
</body>
</html>